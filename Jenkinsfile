node ('workstation') {


  properties([
    parameters([
      string(name: 'harshitha', value: 'left-hand', description: 'one of the best left-hand')
    ])
  ])
  stage('build'){
    perform steps
  }
  stage('Test'){
    perform steps
  }
  stage('Deploy'){
    perform steps
  }
}